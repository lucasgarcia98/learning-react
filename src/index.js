import React from 'react';
import ReactDOM from 'react-dom/client';

import RequestGet from './component/get';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<RequestGet />);